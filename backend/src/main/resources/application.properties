spring.application.name=afc
spring.profiles.active=${SPRING_PROFILES_ACTIVE:local}
server.port=${PORT:8080}

# ── Brapi ─────────────────────────────────────────────────────────────────────
brapi.token=${BRAPI_TOKEN:demo20}
